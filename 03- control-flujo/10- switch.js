let accion = 'guardar';
switch (accion) {
    case 'listar' :
        console.log('Escojiste listar');
        break;
    case 'guardar' :
        console.log('Escojiste guardar');
        break;
    default:
        console.log('No escojiste ninguna de las anteriores')
}